<?xml version="1.0" encoding="utf-8"?>
<resources>
  <style name="AppTheme" parent="@style/Maui.MainTheme">
  </style>
  <!-- Color de marca para el tema (usado por el splash) -->
  <!-- Si ya tenés colors.xml, podés referenciar @color/brand_primary en vez de hardcodear -->
  <!--<color name="brand_primary">#153C46</color>-->

  <!-- Tema del SPLASH: controla barras mientras se muestra el splash -->
  <!--<style name="Maui.SplashTheme" parent="@style/Theme.MaterialComponents.DayNight.NoActionBar">
    --><!-- Importante: mantener oscuro para iconos claros --><!--
    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowLightNavigationBar">false</item>

    --><!-- Barras con color de marca durante el splash --><!--
    <item name="android:statusBarColor">@color/brand_primary</item>
    <item name="android:navigationBarColor">@color/brand_primary</item>

    --><!-- No forzar dark-mode --><!--
    <item name="android:forceDarkAllowed">false</item>
  </style>-->

  <!-- Tema principal de la app (post splash). El color de barras lo seteás en App.xaml.cs -->
  <!--<style name="Maui.MainTheme" parent="@style/Theme.MaterialComponents.DayNight.NoActionBar">
    <item name="android:windowBackground">@android:color/white</item>
    <item name="android:forceDarkAllowed">false</item>
    --><!-- Las dejamos transparentes; luego App.xaml.cs las pone a brand --><!--
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowLightNavigationBar">false</item>
  </style>-->
</resources>
